"""
core — Pure algorithms.  Zero I/O, zero global state, zero side-effects.

Every function in this package takes numpy arrays (or simple Python types) in
and returns numpy arrays (or dataclasses) out.  Nothing here touches the
filesystem, the network, or CUDA environment variables.
"""
